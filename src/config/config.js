const config = {
  username: "rafacoper",
  password: "DrRl*2023#",
  database: "exke",
  host: "localhost",
  dialect: 'postgres',
};

module.exports = {
  development: config,
  test: config,
  production: config,
};
